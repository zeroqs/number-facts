import React from 'react';
import Header from "../components/Header";

const Home = () => {
    return (
        <div className="dark:bg-gray-900">
            <Header/>
        </div>
    );
};

export default Home;
